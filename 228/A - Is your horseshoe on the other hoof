#include<bits/stdc++.h>
using namespace std;
 
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
 
    map<int, int> horseshoes;
 
    for(int i = 0; i < 4; i++) {
        int num;
        cin >> num;
 
        horseshoes[num]++;
    }
 
    cout << 4 - horseshoes.size();
 
    return 0;
}