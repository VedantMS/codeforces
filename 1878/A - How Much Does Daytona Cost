#include <bits/stdc++.h>
using namespace std;
 
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
 
    int t;
    cin >> t;
 
    while (t--) {
        int n, k;
        cin >> n >> k;
 
        string ans = "NO";
 
        for(int i = 0; i < n; i++) {
            int num;
            cin >> num;
 
            if(num == k)        ans = "YES";
        }
 
        cout << ans << '
';
    }
 
    return 0;
}